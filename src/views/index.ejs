<!DOCTYPE html>
<html>

<head>
  <title><%= title %></title>
  <link rel='stylesheet' href='/stylesheets/style.css' />
</head>

<body>
  <h1><%= title %></h1>
  <p>EJS Welcome to <%= title %></p>
  <p><%= msg %></p>
  <p><%= locals.name %></p>
  <div>
    <% if(isAdmin){ %>
    <a>@ 提到我的（2）</a>
    <% } else{ %>
    <button href="#">关注</button>
    <% } %>
  </div>
</body>

</html>